# GD_WG26 吸血鬼幸存者原型 — 局外内容策划案（M9）

> 本文档定位：在 v2 稿（M7 关卡化 + M8 美术化）基础上，为"局外内容（Meta）"规划一版**策划 + 技术落地方案**。
> 目的：把"一局游戏"包裹进完整的**启动 → 主菜单 → 选难度 → 游戏 → 结算 → 返回主菜单**流程，并支持**难度选择、设置、进度存档、退出**。
> 适用引擎：Godot 4.7.1（stable）。
> 约定：沿用 v2 稿"数据驱动、信号解耦、最小侵入"原则。

---

## 〇、当前现状与缺口

对照 v2 稿，游戏目前是"**直接进局**"形态：

| 现状 | 问题 |
|------|------|
| 主场景 `main.tscn` 即 `run/main_scene`，F5 直接开局 | 无主菜单，玩家无法选择难度/看设置 |
| 死亡 / 胜利后弹出 `GameOverPanel`，点击"重新开始"立刻重开 | 无"返回主菜单"入口，局外流程缺失 |
| 难度参数硬编码在 `main.tscn`（`boss_time=720`、`elite_interval=180` 等） | 无法按难度切换数值 |
| 无设置（音量/画质/键位）、无进度存档 | 体验不完整 |

**目标**：增加一套**局外内容（Meta）**——主菜单、难度选择、设置、退出、进度存档，让游戏有"入口与出口"。

---

## 一、局外流程总览

```
应用启动
   │
   ▼
┌────────────┐  ┌────────────┐  ┌────────────┐  ┌────────────┐
│  主菜单     │──►│ 选难度      │──►│  游戏      │──►│  结算      │
│ Title      │  │ Difficulty │  │  main.tscn │  │ 结算面板   │
│ · 开始游戏  │  │ · 简单/普通 │  │            │  │ · 返回菜单  │
│ · 设置      │  │ · 困难      │  │            │  │ · 再来一局  │
│ · 退出      │  └────────────┘  └────────────┘  └────────────┘
└─────┬──────┘        ▲                                     │
      │ 设置也可从主菜单进入                             死亡/胜利
      │                                                   │
      └──────────────►┌────────────┐◄─────────────────────┘
                      │  设置      │
                      │ Settings   │
                      └────────────┘
```

**关键决策：选难度是"开始游戏前"的一次性步骤**，不是每局可中途切换；设置可在主菜单与结算都进入。

**难度解锁规则**：必须**通关（击败 BOSS 胜利）上一难度**才能解锁下一难度；**初始仅显示"简单"**。解锁状态写入存档：`normal` 需通关 `easy` 解锁，`hard` 需通关 `normal` 解锁。

---

## 二、难度设计（3 档）

以 v2 稿 M7 的**普通难度为基准**（即当前 `main.tscn` 默认参数），简单/困难在基准上做整体倍率缩放。

### 2.1 三档难度总览

| 档位 | 定位 | 敌人血量倍率 | 刷怪间隔倍率 | 精英/BOSS 血量倍率 | 经验倍率 | 掉落/成长手感 |
|------|------|------------|------------|------------------|---------|--------------|
| 简单 | 上手 / 熟悉机制 | 0.7 | 1.3（更稀疏） | 0.8 | 1.3 | 升级快、压力低 |
| 普通 | 标准体验（默认） | 1.0 | 1.0 | 1.0 | 1.0 | 当前手感 |
| 困难 | 挑战 / 高手 | 1.5 | 0.8（更密集） | 1.6 | 0.8 | 升级慢、压力高 |

> **原则（呼应初稿 7.3 数值爆炸）**：难度只做"线性倍率 + 数量密度"调整，**不改变成长曲线公式**（`exp_to_next = exp_to_next*1.25+3` 保持不变），避免三档在后期出现不可控的指数差。

### 2.2 各档对既有系统的映射

| 系统 | 简单 | 普通 | 困难 |
|------|------|------|------|
| 普通怪血量（Grunt 20） | ×0.7 = 14 | ×1.0 = 20 | ×1.5 = 30 |
| 精英怪血量（10x 基准） | ×0.8 = 160 | ×1.0 = 200 | ×1.6 = 320 |
| 关底 BOSS 血量（20x 基准） | ×0.8 = 320 | ×1.0 = 400 | ×1.6 = 640 |
| BOSS 刷出时间 | 第 12 分钟 | 第 12 分钟 | 第 12 分钟 |
| 精英刷出间隔 | 每 3 分钟 | 每 3 分钟 | 每 3 分钟 |

> BOSS/精英的**刷出时间点三档一致**（保证规则可预期），差异体现在强度与密度上。

---

## 三、技术落地方案（Godot 4.7）

### 3.1 数据驱动：`DifficultyConfig`（新 Resource）

新增 `src/data/difficulty_config.gd`：

```gdscript
class_name DifficultyConfig extends Resource
## 难度配置（局外内容 M9）。三档难度各自一份 .tres，数据驱动不改逻辑。

@export var id: String = "normal"           ## 唯一标识：easy / normal / hard
@export var display_name: String = "普通"
@export var description: String = "标准体验"
@export var enemy_health_mult: float = 1.0   ## 普通怪血量倍率
@export var enemy_speed_mult: float = 1.0    ## 敌人移速倍率
@export var spawn_interval_mult: float = 1.0 ## 刷怪间隔倍率（>1 更稀疏）
@export var elite_boss_health_mult: float = 1.0  ## 精英/BOSS 血量倍率
@export var exp_mult: float = 1.0            ## 全局经验倍率
@export var max_enemies: int = 300           ## 场上敌人上限（困难可提高）
```

> **落点映射**：`enemy_health_mult / spawn_interval_mult` 作用于 `WaveManager`（刷怪时乘到 `EnemyData` 与 `_current.spawn_interval`）；`elite_boss_health_mult` 作用于精英/BOSS 生成；`exp_mult` 作用于 `GameManager` 宝石掉落或 `PlayerStats.gain_exp`。

### 3.2 流程状态：`GameFlow`（自动加载单例）

新增 `src/game_flow.gd`，设为 **AutoLoad（自动加载单例）**，统一管理"局内 / 局外"场景切换与当前难度：

```gdscript
extends Node
## GameFlow：局外内容流程状态（M9）。自动加载单例。
## 负责：主菜单/游戏/设置场景切换、当前难度存取、局内信息透传。

signal difficulty_changed(cfg: DifficultyConfig)
signal back_to_menu_requested

var current_difficulty: DifficultyConfig

func set_difficulty(cfg: DifficultyConfig) -> void:
	current_difficulty = cfg
	difficulty_changed.emit(cfg)

func start_game() -> void:
	get_tree().change_scene_to_file("res://src/main.tscn")

func go_to_menu() -> void:
	get_tree().change_scene_to_file("res://src/menu/main_menu.tscn")
```

> **设计取舍**：难度通过 `GameFlow.current_difficulty` 在切场景后由 `main.gd` 读取并应用到 `WaveManager`，避免把难度参数硬编码进 `main.tscn`（呼应 3.1 数据驱动）。

### 3.3 场景划分

```
src/
├── menu/
│   ├── main_menu.tscn / .gd        # 主菜单：开始 / 设置 / 退出
│   ├── difficulty_select.tscn/.gd  # 选难度：3 档卡片 → 开始
│   └── settings_panel.tscn/.gd     # 设置：音量 / 画质 / 操作 / 存档
├── data/
│   ├── difficulty_config.gd        # 难度配置 Resource
│   ├── difficulty_easy.tres
│   ├── difficulty_normal.tres
│   └── difficulty_hard.tres
├── game_flow.gd                    # AutoLoad 流程单例
```

### 3.4 关键接线改动

| 文件 | 改动 |
|------|------|
| `project.godot` | `run/main_scene` 改为 `res://src/menu/main_menu.tscn`；注册 `GameFlow` 为 AutoLoad |
| `src/main.gd` | `_ready` 读取 `GameFlow.current_difficulty` 应用到 `WaveManager`（血量/间隔/BOSS倍率/经验）；结算面板新增"返回主菜单"按钮 |
| `src/game_over_panel.gd` | 新增 `menu_requested` 信号 → `GameFlow.go_to_menu()` |
| `src/wave_manager.gd` | 接收 `DifficultyConfig`，刷怪/精英/BOSS 应用对应倍率 |
| `src/game_manager.gd` | 经验掉落应用 `exp_mult`；记录本局最终等级/存活时间用于结算 |

---

## 四、设置页（4 类）

设置通过 **`Settings` 自动加载单例 + `ConfigFile`（`user://settings.cfg`）持久化**，重启保留。

| 分类 | 项 | 实现要点 |
|------|-----|---------|
| **音量** | 主音量 / 音乐 / 音效 | `SfxManager` 用 `AudioServer` 的 `set_bus_volume_db`；全局总线 `Master/Music/SFX` |
| **画质** | 分辨率 / 全屏 / 画质档位 | `DisplayServer.window_set_size`、`window_set_mode(Window.MODE_FULLSCREEN)`；`rendering_method` 由 project.godot 决定，档位只调内部缩放 |
| **操作** | 自定义键位（上下左右） | 写 `InputMap` 运行时替换 `move_*` 的 `InputEventKey`；持久化到 `settings.cfg` |
| **存档** | 最高分显示 / 重置进度 | 读 `user://save.cfg` 显示最佳存活时间/等级；提供"重置进度"按钮（清空该文件） |

> **交互**：设置面板从主菜单与结算面板均可进入；修改即时生效并写盘，关闭面板返回上一级。

---

## 五、进度存档

### 5.1 记录内容

| 字段 | 说明 |
|------|------|
| `best_survive_time` | 最佳存活时间（秒） |
| `best_level` | 最佳等级 |
| `wins` / `games` | 胜利 / 总局数（可选） |
| `unlocked_difficulty` | 已解锁难度（**初始仅解锁简单**；通关简单解锁普通，通关普通解锁困难） |

### 5.2 存储方案

- 用 `ConfigFile` 或 JSON 存 `user://save.cfg`
- `GameFlow` 提供 `record_result(time, level)` 接口，结算面板调用后写盘
- 主菜单显示最高分；选难度界面根据解锁状态置灰未解锁档位

---

## 六、验收标准（M9 Done）

- [ ] 启动进入**主菜单**（标题 + 开始 / 设置 / 退出），不再是直接开局。
- [ ] 主菜单点"开始"→ **选难度**，初始仅显示"简单"，通关后按序解锁"普通"→"困难"，未解锁档位置灰不可选。
- [ ] 三档难度数值差异生效（血量/密度/经验/BOSS 倍率符合 2.2 表）。
- [ ] 死亡/胜利结算后，可**返回主菜单**或**再来一局**（沿用上次难度）。
- [ ] **设置**：音量、画质、键位修改即时生效且重启保留；**退出**按钮可关闭应用。
- [ ] **存档**：记录最佳存活时间/等级，重开后仍显示；可重置进度。
- [ ] 通过 Godot 4.7.1 headless 完整导入校验，无脚本/资源错误。

---

## 七、开发顺序建议

1. **先做流程骨架**：`GameFlow` 单例 + 主菜单/选难度/设置三个空面板 + `main_scene` 切换。
2. **再做难度数据**：三份 `DifficultyConfig.tres` + `WaveManager` 读取应用。
3. **再做设置持久化**：`Settings` 单例 + 音量/画质/键位。
4. **再做存档**：最高分 + 解锁 + 重置。
5. **最后联调**：headless 校验 + 手动走完整流程。

---

## 附：与 v2 稿 / 初稿的关系

| 来源 | 落点 |
|------|------|
| v2 稿 M7 关卡化 | 难度对精英/BOSS/血量的倍率在此之上叠加 |
| 初稿 2.2 数据驱动 | `DifficultyConfig` / `Settings` 均 Resource/配置驱动 |
| 初稿 3.4 波次 | `spawn_interval_mult` 作用于 `WaveManager` |
| 初稿 7.3 数值爆炸 | 三档只做线性倍率，不动成长曲线 |
| 初稿 7.5 暂停处理 | 设置面板需 `PROCESS_MODE_ALWAYS`（若在暂停的结算面板中打开） |
