# GD_WG26 吸血鬼幸存者原型 — 开发方案 v2 稿

> 本文档基于 `初稿.md`（架构与开发顺序蓝图）与项目当前实现状态编写，定位为 **v2 方案稿**。
> 目的：在已完成的 M1~M6 基础上，明确"下一步要做什么、为什么做、怎么做"。
> 适用引擎：Godot 4.7.1（stable）。

---

## 〇、当前进度盘点（截至 v2 起草）

对照 `初稿.md` 第六节的 M1~M6 与 README：

| 里程碑 | 状态 | 已实现要点 |
|--------|------|-----------|
| M1 最小原型 | ✅ | 玩家移动、敌人追击、0.6s 无敌帧、血条/时间 HUD、死亡重开 |
| M2 自动攻击 | ✅ | WeaponManager + 最近敌人查找节流（0.15s）+ 飞弹 + 穿透 |
| M3 经验升级 | ✅ | 宝石掉落 + 吸附拾取 + 升级三选一（暂停）+ PlayerStats 加成 |
| M4 波次系统 | ✅ | WaveManager 时间驱动、难度倍率递增、Wave 2/4 混入快怪/精英 |
| M5 内容填充 | ✅ | 3 武器（发射/光环/近战）、3 敌人（Grunt/Wraith/Brute）、12 项升级 |
| M6 优化打磨 | ✅ | ObjectPool（子弹+宝石）、碰撞优化、屏幕震动、浮动数字、运行时合成音效 |

**结论**：最小可玩闭环（移动→打怪→掉宝→升级→死亡重开）已完整跑通，性能与手感反馈也已具备。当前处于"**功能闭环完成、但仍是原型形态**"的阶段。

### 0.1 当前与"完整游戏"的差距（缺口分析）

对照 `初稿.md` 第一节"核心循环"与第七节"常见坑"，仍存在以下明确缺口：

1. **缺少明确的胜负目标（核心缺口）**
   - 初稿核心循环第 6 条明确要求"死亡或存活到指定时间结束"，但当前**只有死亡**，HUD 仅显示时间、无结束判定，也无"胜利"概念。
   - 这是从"原型"走向"可玩关卡"最关键的一步。

2. **美术仍是色块占位**
   - 玩家/敌人/子弹/宝石全部为 `Polygon2D` / `ColorRect`（美术设计文档 4.5 计划在 M5 启动正式美术，但实际未落地）。
   - 已在 `美术设计文档.md` 准备好完整 AI 出图 Prompt 与导入规范，可与下一步并行。

3. **波次到 Wave 5+ 后难度封顶**
   - `wave_configs.tres` 共 5 档，`wave_duration=30s`，之后一直用最后一档（health_mult=2.5），靠堆数量+精英混刷维持压力，缺少真正的后期 Boss / 阶段化高潮。

4. **校验状态存疑**
   - `logs/verify_result.txt` 显示 `VERIFY_FAIL`（多个 `.tscn/.tres` LOAD FAIL），但 README 称 M5/M6 已通过 headless 导入校验，且 `logs/verify.log`、`godot_import.log` 为空。
   - 需先确认是校验脚本（如 `_verify.gd.uid` 指向的脚本）本身问题，还是资源路径/UID 漂移导致的真实失败。**这是下一步动手前的第一件事**。

---

## 一、下一步总目标（v2 阶段）

把原型推进为**有清晰目标的关卡（M7）+ 正式美术落地（M8）**，分两条并行线：

- **主线（M7 关卡化）**：补上"胜负目标 + 阶段化难度 + 结算"，让一局游戏有明确起点与终点。
- **支线（M8 美术化）**：用 `美术设计文档.md` 的 Prompt 批量产出贴图，替换色块，按 4.x 规范导入，不改动手感与逻辑。

建议**先 M7 再 M8**（先定玩法目标，再换皮），因为美术替换依赖实体接口稳定。但 M8 资源生产可与 M7 编码并行。

---

## 二、M7：关卡化与胜负目标（下一步主线）

### 2.1 目标定义

一局游戏 = "在限定时间内存活并击败阶段 Boss"。具体：

- **存活目标**：默认 `RUN_DURATION = 15 分钟`（900s），可配置；时间到即触发"胜利结算"。
- **阶段 Boss**：在固定时间点（如第 5、10、15 分钟）刷出一只强化 Boss（基于 `enemy_boss.tres`，大幅提高血量/伤害，带专属外观色/名字）。
- **结束条件**：玩家死亡（失败）或存活到 `RUN_DURATION`（胜利）。两种都进入结算面板。

### 2.2 数据驱动改动（遵循初稿 2.2）

在 `WaveTable` / `WaveConfig` 基础上新增，不破坏现有结构：

- `RunConfig`（新 Resource，`src/data/run_config.gd`）：
  - `run_duration: float = 900.0`
  - `boss_waves: Array[BossSpawn]`：`{time: float, enemy_data: EnemyData, label: String}`
  - 复用 `WaveManager` 的 `wave_duration` 概念，新增按"绝对时间"触发 Boss。

### 2.3 代码改动清单（尽量小、信号解耦）

| 文件 | 改动 |
|------|------|
| `src/data/run_config.gd`（新） | 运行配置 Resource |
| `src/wave_manager.gd` | 接收 `RunConfig`；新增 `_boss_timer` 按绝对时间刷 Boss；发出 `boss_spawned` 信号 |
| `src/game_manager.gd` | 新增 `victory` 信号；监听存活时间达成或 Boss 全清→触发胜利；死亡仍发 `game_over`（失败） |
| `src/main.gd` | 连接 `game_over`/`victory` 到结算面板；`_start_run` 读取 `RunConfig` 重置计时 |
| `src/ui/game_over_panel.gd` | 复用为"结算面板"，区分 `show_victory()` / `show_defeat()`，显示存活时间、等级、击杀数 |
| `src/ui/hud.gd` | 时间条改为"距胜利倒计时"或进度条；显示 Boss 警告提示 |
| `src/player/player.gd` | 无需大改，死亡逻辑不变 |

> **注意**：升级暂停用 `get_tree().paused`，Boss 刷出与胜利判定要在 `process_mode=ALWAYS` 节点或计时器上处理（初稿 7.5 坑），避免被暂停冻结。

### 2.4 难度曲线（避免数值爆炸，呼应初稿 7.3）

- 现有 `exp_to_next = exp_to_next * 1.25 + 3` 已控制成长，保留。
- Boss 血量用"基础 × 时间倍率"，时间倍率随 `RUN_DURATION` 平滑，而非无限指数。
- 后期（Wave 5+ 之后）在 `RunConfig` 里继续小幅提升 `health_mult`（如每 2 分钟 +0.3），但不靠单纯堆数量（受 `max_enemies=300` 限制）。

### 2.5 验收标准（M7 Done）

- [ ] 玩家死亡 → 失败结算面板，可重开。
- [ ] 存活至 `RUN_DURATION` → 胜利结算面板，可重开。
- [ ] 固定时间点刷出阶段 Boss，HUD 有警告，Boss 死亡有反馈。
- [ ] 暂停/升级期间 Boss 计时与胜利判定不被错误冻结。
- [ ] 通过 Godot 4.7.1 headless 完整导入校验（先解决 0.1-4 的 VERIFY_FAIL）。

---

## 三、M8：正式美术落地（下一步支线，与 M7 并行生产）

严格按 `美术设计文档.md` 执行，核心约束：

1. **不改动碰撞与手感**：只把 `Polygon2D` 换成 `Sprite2D`/`AnimatedSprite2D`，碰撞形状（`CollisionShape2D`）维持几何不变（文档 4.5）。
2. **数据驱动外观**：在 `EnemyData` / `WeaponData` 增加 `@export var texture: Texture2D`（初稿 2.2、美术文档 4.5 已预告），实体 `_ready` 里 `Sprite2D.texture = data.texture`，逻辑不感知贴图。
3. **先锁调色板再出图**（文档 1.3 调色板），用 3.1~3.6 的 Prompt 批量产出。
4. **资产目录**：按 4.4 规范放入 `res://assets/characters|items|projectiles|ui|vfx|bg`。
5. **性能**：小物（宝石/子弹）用 `AtlasTexture` 合图；背景用 `TileMapLayer` 平铺；避免半透明大面积 overdraw（4.2）。

> M8 优先级低于 M7：美术不影响可玩性，可在 M7 玩法目标确定后集中替换，避免中途接口变动导致返工。

---

## 四、立即行动清单（下一步第一周）

按依赖顺序：

1. **[阻塞项] 排查 VERIFY_FAIL**：确认 `verify_result.txt` 是校验脚本问题还是真实资源加载失败。运行环境配置里的 headless 校验命令复测；若真实失败，按初稿 7.1/7.4 检查碰撞层与 UID。
2. **[M7-设计] 确定 RunConfig 数值**：`run_duration`、Boss 时间点与倍率，先填一套再游戏内实测。
3. **[M7-编码] 实现胜负目标**：按 2.3 清单改动，最小侵入、信号解耦。
4. **[M8-生产] 启动美术出图**：用美术文档 Prompt 产出玩家/3 敌人/宝石/子弹/图标全套，按 4.4 入库（可与 3 并行，因为不依赖代码）。
5. **[M7-验收] headless 校验 + 手动试玩**：确认胜负闭环与暂停正确性。

---

## 五、v2 之后（M9+，可选方向，不在本次范围）

- **武器/敌人扩展**：近战挥砍 `melee_effect.tscn` 已建场景但未在升级池解锁（当前只有发射/光环），可补"近战武器解锁"升级项。
- **Boss 行为**：当前 Boss 仍是纯追击，可加冲刺/弹幕等阶段技能（新 `BossData` + 行为状态机）。
- **存档/最高分**：本地记录最佳存活时间/等级。
- **设置面板**：音量（SfxManager 已合成）、画质档位。

> 原则不变（初稿结语）：**先跑通最小原型，再不断加内容；数据驱动、信号解耦、AI 辅助提效**。

---

## 附：与初稿/美术文档的对应关系

| 初稿/美术文档条目 | v2 落点 |
|-------------------|---------|
| 初稿 一.6 "存活到指定时间结束" | M7 胜负目标（当前唯一未实现的核心循环条目） |
| 初稿 2.2 数据驱动 | RunConfig / EnemyData.texture 扩展 |
| 初稿 3.4 波次系统 | M7 Boss 按绝对时间触发 |
| 初稿 7.3 数值爆炸 | M7 `RUN_DURATION` 平滑倍率 |
| 初稿 7.5 暂停处理 | M7 胜利/Boss 计时用 ALWAYS 节点 |
| 美术文档 4.5 美术替换 | M8 Sprite2D 替换 Polygon2D |
| 美术文档 3.x AI Prompt | M8 出图直接套用 |
