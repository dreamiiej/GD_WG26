# 美术设计文档（Art Design Doc）

> 本篇作为 `初稿.md` 的补充，专门讨论美术资源的获取与设计方案、AI 生成词（Prompt）建议，以及美术制作规范。
> 适用项目：Godot 4.x 吸血鬼幸存者类（Vampire Survivors-like）游戏。
> 目标：在保持"先用色块跑通原型"的基础上，给出可落地的美术落地路线，既能用 AI 快速产出，也能对接人工精修。

---

## 一、整体美术方向与风格定位

### 1.1 风格选型建议

吸血鬼幸存者类游戏屏幕上存在大量实体（数百敌人 + 子弹 + 掉落物），美术风格必须兼顾：

- **辨识度高**：玩家、敌人、子弹、经验宝石在小尺寸下也要一眼区分。
- **批量渲染友好**：避免复杂描边/半透明叠加，降低 GPU 压力。
- **产出效率高**：能用 AI 批量生成，也能用色块/几何占位。

推荐方向（按优先级）：

| 风格 | 优点 | 风险 | 适用阶段 |
|------|------|------|----------|
| 扁平矢量（Flat Vector） | 辨识度高、文件小、易批量 | 略显简单 | 全阶段首选 |
| 像素风（Pixel Art） | 复古、性能极佳、玩家接受度高 | AI 出图易"伪像素"，需精修 | 正式美术 |
| 低多边形 2D（Low-poly/Geometric） | 与初稿色块方案无缝衔接 | 表现力有限 | 原型→正式过渡 |
| 暗黑哥特（Dark Gothic） | 契合"吸血鬼"主题 | 易显脏、难统一 | 主题定型后 |

**建议**：原型期沿用初稿的色块/几何；正式美术期采用"扁平矢量 + 暗色背景"的组合，既有吸血鬼幸存者味，又能 AI 批量产出且性能友好。

### 1.2 视觉基调

- **背景**：深色（近黑/深蓝紫），让亮色实体跳出来。
- **玩家**：高饱和暖色（橙/黄），始终最显眼。
- **敌人**：冷色（红/紫）为主，按类型用色相区分强弱。
- **经验宝石**：青绿/亮蓝，与敌人冷色拉开，避免误判。
- **子弹/特效**：白、黄、品红等高亮色，短促醒目。

### 1.3 调色板（建议锁定一套主色，避免后期杂乱）

```
玩家主色      #FFB347 (橙)
玩家高光      #FFE0A3
敌人-弱       #C0392B (暗红)
敌人-中       #8E44AD (紫)
敌人-强       #2C3E50 (深蓝灰) + 红描边
经验宝石      #2ECC71 (绿)
子弹-基础     #FFFFFF
子弹-火       #FF6B35
危险/血条     #E74C3C
背景          #14101F (深紫黑)
UI 文本       #F5F5F5
```

> 在 Godot 中可把这些存为 `theme_override_colors` 或统一主题资源，保证全项目一致。

---

## 二、美术资源清单（Asset List）

按实体/界面拆解，标注"原型占位 → 正式美术"的迁移路径。

### 2.1 角色与实体

| 资源 | 原型占位 | 正式美术 | 尺寸建议 | 动画需求 |
|------|----------|----------|----------|----------|
| 玩家角色 | 橙色圆/三角 | 扁平矢量人形 | 32×32 / 48×48 | 待机、移动（2~4 帧） |
| 敌人-弱（小怪） | 暗红方块 | 扁平矢量小怪 | 24×24 | 移动循环 |
| 敌人-中（快速） | 紫色菱形 | 扁平矢量飞行体 | 28×28 | 移动 + 拖尾 |
| 敌人-强（精英/Boss） | 深蓝大圆 | 扁平矢量精英 | 64×64 / 96×96 | 移动 + 受击 + 死亡 |
| 经验宝石 | 绿色小三角 | 发光宝石 | 16×16 | 轻微脉冲 |
| 武器子弹-基础 | 白色小圆 | 光弹 | 12×12 | 拖尾 |
| 武器子弹-范围 | 半圆/环 | 光环 | 视范围 | 扩散/旋转 |
| 近战挥砍 | 弧线色块 | 刀光弧 | 视范围 | 快速出现消失 |

### 2.2 环境与 UI

| 资源 | 说明 | 备注 |
|------|------|------|
| 背景平铺图 | 深色纹理/网格 | 可用程序化或 AI 生成后平铺 |
| 地面装饰 | 散落石子/血迹 | 低密度，避免抢视线 |
| HUD 框架 | 血条、经验条、计时器 | Godot Control 节点绘制 |
| 升级面板卡片 | 三选一卡片底图 | 矢量 + 图标 |
| 图标（武器/被动） | 每种升级项一个图标 | 32×32 扁平图标 |
| 游戏结束/暂停界面 | 半透明遮罩 + 文本 | Control 节点 |

### 2.3 特效（VFX）

- 击中火花（小闪烁）
- 敌人死亡碎裂（粒子）
- 升级闪光
- 玩家受伤红屏/无敌闪烁
- Boss 出场暗角

> 原型期全部用 `Polygon2D`/粒子色块代替，正式期再替换贴图。

---

## 三、AI 生成词（Prompt）建议

下面给出可直接套用的中英文 Prompt 模板。通用原则：

1. **明确风格与视角**：2D game asset, flat vector, top-down / side view, transparent background。
2. **明确尺寸与构图**：small icon, centered, full body, no background。
3. **控制变量**：一次只改一个描述词，方便横向对比挑选。
4. **避免"伪像素"**：pixel art 务必加 `crisp pixel, 32x32 grid, limited palette`，否则 AI 容易生成模糊小图。
5. **透明背景**：加 `transparent background / isolated on white` 便于直接导入 Godot。

### 3.1 玩家角色

```
English:
2D game character sprite, flat vector style, top-down view, a brave survivor
with a cloak, orange and yellow color scheme, simple geometric shape,
transparent background, clean silhouette, small size, readable at 48x48,
minimal details, no shading.

中文（给中文模型）:
2D 游戏角色精灵，扁平矢量风格，俯视角，身穿斗篷的幸存者，
橙黄配色，简洁几何造型，透明背景，轮廓清晰，小尺寸，48x48 可辨识，
细节少，无复杂阴影。
```

### 3.2 敌人（按类型）

```
弱怪（暗红小怪）:
2D game enemy, flat vector, top-down, small red blob monster with angry eyes,
dark red palette, simple, transparent background, menacing but tiny, 24x24.

中怪（紫色飞行体）:
2D game enemy, flat vector, top-down, purple flying wraith with trailing wisps,
glow accent, transparent background, sleek shape, 28x28.

精英/Boss（深蓝强壮）:
2D game boss, flat vector, top-down, large armored dark-blue brute with red
outline, heavy and threatening, transparent background, 96x96, bold silhouette.
```

### 3.3 经验宝石 / 掉落物

```
2D game item icon, flat vector, glowing green gem diamond shape,
bright cyan-green, transparent background, small, pixel-clean edges, 16x16,
emits soft light.
```

### 3.4 子弹与特效

```
基础光弹:
2D game projectile, flat vector, white glowing orb with motion trail,
transparent background, small, 12x12, clean.

火属性弹:
2D game projectile, flat vector, orange fire ball with flame trail,
transparent background, 16x16.

击中火花:
2D game VFX, flat vector, small white-yellow spark burst, 4 directions,
transparent background, no background.
```

### 3.5 图标（升级项）

```
2D game UI icon, flat vector, [武器/被动名称，如 "lightning bolt" / "heart shield"],
single color accent on dark rounded square, transparent background, 32x32,
minimal, high contrast.
```

### 3.6 背景

```
2D game background, dark purple-black, subtle tileable stone floor texture,
top-down, faint grid, no bright elements, atmospheric, horror survival mood,
seamless tiling.
```

### 3.7 给 AI 出图的批量流程建议

1. 先出**一张风格设定稿（style sheet）**，确认整体调性。
2. 用同一套风格词，逐个生成实体，保证一致性。
3. 对不满意的，用 `variant of previous, change color to X` 局部改。
4. 生成后用工具（见第五节）统一去背、缩放、导出。

---

## 四、美术制作规范（Production Spec）

### 4.1 通用规范

- **画布与导出**：源文件用 2 的幂尺寸（32/64/128/256），导出 PNG 带透明通道。
- **分辨率策略**：Godot 项目设置 `rendering/scaling` 建议用 `viewport` 或 `integer_scaling`，美术按最终显示尺寸 × 设计缩放倍率（如 2x/3x）绘制，避免放大模糊。
- **锚点与原点**：角色/敌人原点设在**中心**，便于旋转与缩放；子弹原点设在中心。
- **调色一致**：所有贴图严格使用第一节锁定的调色板，禁止随手取色。
- **命名规范**（见 4.4）。

### 4.2 性能相关规范

- 单张贴图尽量 ≤ 256×256；大量同类敌人共用一张图 + `AnimatedSprite2D` 帧。
- 避免半透明大面积叠加（敌人多时会爆 overdraw）；实体描边用实色而非模糊描边。
- 经验宝石、子弹等小物可用 `AtlasTexture` / `SpriteFrames` 合图，减少 draw call。
- 背景用 `TextureRect` 平铺或 `TileMapLayer`，不要放几百个 Sprite。

### 4.3 动画规范

- 角色移动循环 ≤ 4 帧，帧率 8~12 fps（复古感且省资源）。
- 死亡/碎裂用粒子或 2~3 帧快速播放后隐藏。
- Boss 动画可到 6~8 帧，但仍保持扁平。
- 所有动画在 Godot `AnimatedSprite2D` 中配置，源图按横向帧排列。

### 4.4 文件与命名规范

```
目录结构（建议放在项目 res:// 下）：
res://assets/
├── characters/
│   ├── player.png
│   ├── enemy_weak.png
│   ├── enemy_fast.png
│   └── enemy_boss.png
├── items/
│   └── gem_exp.png
├── projectiles/
│   ├── bullet_base.png
│   └── bullet_fire.png
├── ui/
│   ├── icon_*.png
│   └── panel_*.png
├── vfx/
│   └── spark_hit.png
└── bg/
    └── floor_tile.png
```

命名规则：`类别_名称_状态或变体.png`
- `player_idle.png` / `player_move.png`
- `enemy_boss_hit.png`
- `icon_lightning.png`

> 与初稿数据驱动呼应：资源路径也写进 `WeaponData`/`EnemyData` 的 `@export` 字段（如 `texture: Texture2D`），实现美术与逻辑解耦。

### 4.5 与初稿架构的衔接

初稿用 `Polygon2D`/`ColorRect` 占位。替换正式美术时：

- 把占位节点换成 `Sprite2D` / `AnimatedSprite2D`，引用 `assets/` 下贴图。
- 碰撞形状（`CollisionShape2D`）维持几何不变，仅外观变化，不影响手感。
- 数据资源（`EnemyData.texture` 等）驱动外观，无需改核心代码——与初稿"数据驱动"原则一致。

---

## 五、美术资源获取渠道与工具

### 5.1 AI 生成工具

| 工具 | 用途 | 备注 |
|------|------|------|
| Midjourney / Niji | 风格设定、角色概念 | 风格统一好，需会员 |
| Stable Diffusion（本地） | 批量生成、可控性强 | 配 ControlNet 锁构图 |
| DALL·E / 通义万相 / 智谱清言 | 快速出图 | 中文友好 |
| 即梦 / 可灵 | 国产、出图快 | 适合快速迭代 |

### 5.2 去背 / 处理工具

- **remove.bg** / **Photoshop 一键去背**：处理透明背景。
- **TexturePacker**：打图集（Atlas），减小 draw call。
- **Aseprite / Piskel**：像素风精修、帧动画（若走像素路线）。
- **Inkscape / Figma**：扁平矢量精修与图标绘制。
- **Godot 内置**：导入后设 `flags`，用 `AtlasTexture` 裁剪。

### 5.3 免费素材库（人工精修或应急）

- **Kenney.nl**：大量免费扁平游戏素材，CC0，可直接用或作参考。
- **OpenGameArt.org**：CC 协议素材，注意授权。
- **itch.io**（免费资产区）：独立作者发布的免费包。

> 注意：AI 生成物与第三方素材均需确认**商用授权**，正式发布前核查许可证。

---

## 六、落地路线（与初稿开发顺序对应）

| 初稿里程碑 | 美术动作 |
|------------|----------|
| M1 最小原型 | 维持色块占位，仅定调色板 |
| M2 自动攻击 | 补子弹/敌人占位色，区分色相 |
| M3 经验升级 | 经验宝石占位 + 升级面板用 Control 绘制 |
| M4 波次 | 引入 1~2 种敌人配色变体 |
| M5 内容填充 | **正式美术启动**：用 AI 出玩家/敌人/武器/图标全套，替换占位 |
| M6 优化打磨 | 打图集、统一特效、背景与 UI 精修 |

**建议**：M5 之前不要花大力气做美术，先让系统跑通；M5 集中用本文 Prompt 批量产出，再按规范导入。

---

## 七、常见美术坑

1. **AI 出图尺寸不一**：导入 Godot 后大小参差，务必统一导出尺寸并按 4.1 规范。
2. **透明通道丢失**：导出时确认 PNG 含 alpha，Godot 导入勾选 `Detect 3D` 无关，但要确认无黑底。
3. **调色失控**：多人/多模型产出容易五花八门，先锁调色板再生成。
4. **overdraw 爆炸**：大量半透明敌人导致帧率掉，参考 4.2 用实色。
5. **动画帧排列错**：Godot `SpriteFrames` 需横向等距排列，源图错排会播放错乱。
6. **忽略原点**：原点不在中心，旋转/缩放时实体会"跳"。
7. **商用授权**：AI 生成与素材库务必确认可商用，避免上线风险。

---

> 本文档与 `初稿.md` 配套使用：初稿负责"系统怎么搭"，本篇负责"东西长什么样、怎么来、怎么规范"。两者都遵循同一原则——**数据驱动、先跑通再精修、AI 辅助提效**。
